--- app/static/app.js
+++ app/static/app.js
@@ -268,6 +268,7 @@
         };
 
         recognition.onend = () => {
+            console.log("[DEBUG] onend: currentTranscript=", currentTranscript, "isRecording=", isRecording);
             if (!currentTranscript && !isRecording) {
                 setStatus("Ready");
             }
